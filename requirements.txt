httpx>=0.27.0
beautifulsoup4>=4.12.0
playwright>=1.44.0
pyyaml>=6.0.0
openpyxl>=3.1.0
pandas>=2.2.0
python-dateutil>=2.9.0
# Traitement d'images (empreinte photo pour la déduplication inter-sources)
Pillow>=10.0.0
numpy>=1.26.0
# Matching qualitatif : déléguée à un LLM local servi par Ollama (conteneur
# dédié, cf. docker-compose.yml + workers/qualitative.py). Appels via httpx
# (déjà listé ci-dessus) → AUCUNE dépendance Python lourde ici (plus de
# sentence-transformers ni torch).
